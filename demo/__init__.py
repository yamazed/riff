"""A loopback-only demo app and traffic generator for exercising riff.

See :mod:`demo.app` — some of its routes are deliberately vulnerable so the
scanner has something to find. It binds 127.0.0.1 and is not for deployment.
"""
